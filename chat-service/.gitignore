node_modules/
dist/
runtime/*
!runtime/seed/
.env
*.log
.DS_Store
seed/starter-verify-progress.json
seed/starter-verification-report.json
# Answer-quality eval per-run trail archives (full SSE transcripts + tool args).
# Large and reproducible; the lean committed snapshot is the scorecard of record.
test/eval/runs/
