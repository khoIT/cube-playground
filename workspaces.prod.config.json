{
  "_comment": "Prod Cube workspace registry. Loaded in the container via WORKSPACES_CONFIG_PATH (see docker-compose.prod.yml) so the local-dev workspaces.config.json stays unchanged. Prod cube-dev is open (no token) and prefix-namespaced; URLs live here, NOT in .env — the gateway resolves workspace id -> cubeApiUrl from this file (SSRF guard).",
  "default": "prod",
  "workspaces": [
    {
      "id": "prod",
      "label": "Prod cube-dev",
      "cubeApiUrl": "https://cube.gds.vng.vn",
      "authMode": "none",
      "gameModel": "prefix",
      "gamePrefixMap": {
        "cfm_vn": "cfm",
        "ballistar": "ballistar",
        "cros": "cros",
        "jus_vn": "jus"
      }
    },
    {
      "_comment": "In-stack cube-dev (docker-compose.prod.yml cube_api service). Reached over the compose network at cube_api:4000 — NOT the published host port (17001 is browser-only). Multi-tenant: authMode=minted so the JWT's game claim selects the tenant schema.",
      "id": "local",
      "label": "Local cube-dev",
      "cubeApiUrl": "http://cube_api:4000",
      "authMode": "minted",
      "gameModel": "game_id"
    }
  ]
}