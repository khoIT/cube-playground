{
  "_comment": "Cube workspace registry — server-side only. Client never sees URLs, only ids.",
  "default": "local",
  "workspaces": [
    {
      "id": "local",
      "label": "Local Workspace",
      "cubeApiUrl": "http://localhost:4000",
      "authMode": "minted",
      "gameModel": "game_id"
    },
    {
      "id": "prod",
      "label": "Production Workspace",
      "cubeApiUrl": "https://cube.gds.vng.vn",
      "authMode": "minted",
      "gameModel": "prefix",
      "allowedRoles": [
        "editor",
        "admin"
      ]
    }
  ]
}